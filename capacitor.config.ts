import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "fr.antoinek.ionicreactboilerplate",
  appName: "ionic-react-boilerplate",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
